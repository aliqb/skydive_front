export interface UserTransaction {
  date: string;
  ticketNumber: string;
  eventName: string;
  paymentInformation: '';
  amount: number;
  type: string;
  invoiceNumber: number;
  id: string;
  createdAt: string;
  updatedAt: string;
}
